# EvalLens data

Run `python ..\scripts\build_evallens_dataset.py --results ..\test_results\<run>` to create `pred.jsonl` and `gt.jsonl`. Generated data may contain source contract quotations and is gitignored.
