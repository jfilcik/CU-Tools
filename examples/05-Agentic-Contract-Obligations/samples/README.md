# Samples

Run `python ..\scripts\download_cuad.py` followed by `python ..\scripts\prepare_cuad_eval.py` from this example directory. Selected CUAD contract text is written to `samples/downloaded/` and is not committed.
