[pytest]
markers =
    unit: offline unit tests
    integration: integration tests requiring explicitly configured dependencies
    slow: longer-running tests
    performance: local performance checks
